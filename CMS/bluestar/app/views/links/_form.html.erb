<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :links_url %><br />
    <%= f.text_field :links_url %>
  </div>
  <div class="field">
    <%= f.label :region %><br />
    <%=  collection_select(:link , :region_id,Region.all,:id,:region_name)   %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%=  collection_select(:link , :type_id, Type.all, :id,:type_name)   %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
