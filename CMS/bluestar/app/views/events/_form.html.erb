<%= form_for(@event,:html => {:multipart => true}) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
    <div class="field">
    <%= f.label :region %><br />
    <%= collection_select(:event, :region_id, Region.all, :id, :region_name)  %>
  </div>
  <div class="field">
    Icon:<br />
    <%= f.file_field :logo %>
  </div>
  <div class="field">
    Logo:<br />
    <%= f.file_field :right_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>