<h1>Listing types</h1>

<table>
  <tr>
    <th>Type name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @types.each do |type| %>
  <tr>
    <td><%= type.type_name %></td>
    <td><%= link_to 'Show', type %></td>
    <td><%= link_to 'Edit', edit_type_path(type) %></td>
 </tr>
<% end %>
</table>

<br />

