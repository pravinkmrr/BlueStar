<%= form_for(@eicon,:html=>{:multipart=>true}) do |f| %>
  <% if @eicon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eicon.errors.count, "error") %> prohibited this eicon from being saved:</h2>

      <ul>
      <% @eicon.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :icon_name %><br />
    <%= f.text_field :icon_name %>
  </div>
  <div class="field">
    <%= f.label :icon %><br />
    <%= f.file_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>