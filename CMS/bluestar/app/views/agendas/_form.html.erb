<%= form_for(@agenda,:html=>{:multipart =>true}) do |f| %>
  <% if @agenda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda.errors.count, "error") %> prohibited this agenda from being saved:</h2>

      <ul>
      <% @agenda.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :pdf %><br />
    <%= f.file_field :pdf %>
  </div>
  <div class="field">
    <%= f.label :event %><br />
    <%= collection_select(:agenda, :event_id, Event.all, :id, :name)  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>