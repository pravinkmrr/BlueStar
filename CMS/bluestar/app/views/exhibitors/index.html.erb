<h1>Listing exhibitors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Pdf</th>
    <th>Event</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @exhibitors.each do |exhibitor| %>
  <tr>
    <td><%= exhibitor.name %></td>
    <td><%= link_to "View", exhibitor.filename %></td>
    <td><%= Event.find(exhibitor.event_id).name %></td>
    <td><%= link_to 'Show', exhibitor %></td>
    <td><%= link_to 'Edit', edit_exhibitor_path(exhibitor) %></td>
    <td><%= link_to 'Destroy', exhibitor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Exhibitor', new_exhibitor_path %>
