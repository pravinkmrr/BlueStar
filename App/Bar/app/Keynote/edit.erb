<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @keynote.name %></h1>
    <a href="<%= url_for :action => :show, :id => @keynote.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @keynote.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @keynote.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="keynote[name]" class="fieldLabel">Name</label>
            <input type="text" id="keynote[name]" name="keynote[name]" value="<%= @keynote.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
          <div data-role="fieldcontain">
            <label for="keynote[filename]" class="fieldLabel">Filename</label>
            <input type="text" id="keynote[filename]" name="keynote[filename]" value="<%= @keynote.filename %>" <%= placeholder( "Filename" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
