<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>New <%= @keynote.title %></h1>
    <a href="<%= url_for :action => :index %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :create %>">
      <input type="hidden" name="id" value="<%= @keynote.object %>"/>

      
          <div data-role="fieldcontain">
            <label for="keynote[name]" class="fieldLabel">Name</label>
            <input type="text" id="keynote[name]" name="keynote[name]" <%= placeholder("Name") %> />
          </div>
      
          <div data-role="fieldcontain">
            <label for="keynote[filename]" class="fieldLabel">Filename</label>
            <input type="text" id="keynote[filename]" name="keynote[filename]" <%= placeholder("Filename") %> />
          </div>
      

      <input type="submit" value="Create"/>
    </form>
  </div>

</div>
