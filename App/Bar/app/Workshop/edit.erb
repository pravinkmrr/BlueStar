<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @workshop.name %></h1>
    <a href="<%= url_for :action => :show, :id => @workshop.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @workshop.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @workshop.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="workshop[name]" class="fieldLabel">Name</label>
            <input type="text" id="workshop[name]" name="workshop[name]" value="<%= @workshop.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
