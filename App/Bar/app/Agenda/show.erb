<div data-role="page" data-add-back-btn="false">

  <div data-role="header" data-position="inline">
    <h1><%= @agenda.filename %></h1>
    <a href="<%= url_for :action => :index %>" class="ui-btn-left" data-icon="arrow-l" data-direction="reverse">
      Back
    </a>
    <a href="<%= url_for :action => :edit, :id => @agenda.object %>" class="ui-btn-right" data-icon="star">
      Edit
    </a>
  </div>

  <div data-role="content">
    <ul data-role="listview">
      
          <li>
            <div class="itemLabel">Filename</div>
            <div class="itemValue"><%= @agenda.filename %></div>
          </li>
      
          <li>
            <div class="itemLabel">EventId</div>
            <div class="itemValue"><%= @agenda.event_id %></div>
          </li>
      
          <li>
            <div class="itemLabel">Name</div>
            <div class="itemValue"><%= @agenda.name %></div>
          </li>
      
    </ul>
  </div>

</div>
