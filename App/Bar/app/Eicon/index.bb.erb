<div data-role="page">

  <div data-role="header" data-position="inline">
    <%= link_to "New", :action => :new %>
    <h1>Eicons</h1>
    <%= link_to "Home", RhoConfig::start_path %>
  </div>

  <div data-role="content">
    <table>
      <tr>
      <% @eicons.each do |obj| %>
        
            <td class="recordLabel"><%= link_to "#{obj.icon_file}", :action => :show, :id => obj.object %></td>
        
      <% end %>
      </tr>
    </table>
  </div>

</div>
